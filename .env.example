# ELS Pipeline Environment Variables
# Copy this file to .env and fill in your values

# AWS Configuration
AWS_REGION=us-east-1
ENVIRONMENT=dev

# Test ENV configuration
COUNTRY=US
STATE=CA
VERSION_YEAR=2021

# S3 Bucket Names (set after CloudFormation deployment)
# Format: els-{bucket-type}-{environment}-{account-id}
ELS_RAW_BUCKET=els-raw-documents-dev-123456789012
ELS_PROCESSED_BUCKET=els-processed-json-dev-123456789012
ELS_EMBEDDINGS_BUCKET=els-embeddings-dev-123456789012

# S3 Path Structure (country-based)
# Raw documents: {country}/{state}/{year}/{filename}
# Processed JSON: {country}/{state}/{year}/{standard_id}.json
# Example: US/CA/2021/california_standards.pdf
S3_PATH_PATTERN={country}/{state}/{year}/{identifier}

# Country Code Validation
# Set to "enabled" to validate ISO 3166-1 alpha-2 country codes
COUNTRY_CODE_VALIDATION=enabled

# Bedrock Model Configuration
# Use cross-region inference profile for Claude Sonnet 4.5
BEDROCK_LLM_MODEL_ID=us.anthropic.claude-sonnet-4-6
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0

# Pipeline Configuration
CONFIDENCE_THRESHOLD=0.7

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=els_corpus
DB_USER=postgres
DB_PASSWORD=

# State machine configuration
STATE_MACHINE_ARN=
TEST_DOCUMENT_PATH=standards/california_all_standards_2021.pdf

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update bucket names with actual values after CloudFormation deployment
# 3. Get bucket names:
#    aws cloudformation describe-stacks --stack-name els-pipeline-dev \
#      --query 'Stacks[0].Outputs[?OutputKey==`RawDocumentsBucketName`].OutputValue' --output text
# 4. Load variables: source .env (or use python-dotenv)
# 5. Country codes must be ISO 3166-1 alpha-2 format (e.g., US, CA, AU, GB)
