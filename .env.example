# ELS Pipeline Environment Variables
# Copy this file to .env and fill in your values

# AWS Configuration
AWS_REGION=us-east-1

# S3 Bucket Names (set after CloudFormation deployment)
# Format: els-raw-documents-{environment}-{account-id}
ELS_RAW_BUCKET=els-raw-documents-dev-123456789012
ELS_PROCESSED_BUCKET=els-processed-json
ELS_EMBEDDINGS_BUCKET=els-embeddings

# Bedrock Model Configuration
BEDROCK_LLM_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v1

# Pipeline Configuration
CONFIDENCE_THRESHOLD=0.7

# Database Configuration (for future use)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=els_corpus
DB_USER=postgres
DB_PASSWORD=

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update ELS_RAW_BUCKET with your actual bucket name after CloudFormation deployment
# 3. Get bucket name: aws cloudformation describe-stacks --stack-name els-pipeline-dev --query 'Stacks[0].Outputs[?OutputKey==`RawDocumentsBucketName`].OutputValue' --output text
# 4. Load variables: source .env (or use python-dotenv)
